<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redirection</title>
</head>

<body>
    <h1>Edit Link</h1>
    <form action="/edit/<%=body.id%>" method="POST" >
    <input class="field" name="title" placeholder="Title" required value="<%=body.title%>">
    <input class="field" name="description" placeholder="Description" required value="<%=body.description%>">
    <input class="field" name="url" placeholder="Url" required value="<%=body.url%>">
    <button>Edit</button>
    </form>

    
    <h3><%=error.message%></h3>
    
</body>

</html>